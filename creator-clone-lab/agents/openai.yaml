interface:
  display_name: "Creator Clone Lab"
  short_description: "Batch-distill creator evidence into reviewed knowledge and an Obsidian graph."
  default_prompt: "Route this creator task, use the Playwright browser-automation skill as the mandatory controller for every web capture, process sources through the resumable extraction queue, generate reviewable promotion, duplicate, and relation candidates, apply only explicit review decisions, then rebuild the creator clone and linked Obsidian vault with complete source citations."
